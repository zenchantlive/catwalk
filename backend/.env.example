# Database (Defaults to local SQLite)
# DATABASE_URL=postgresql://user:password@localhost:5432/mcp_remote

# Security (Required for Credentials)
# Run `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"` to generate one
ENCRYPTION_KEY=

# LLM Provider (Available: https://openrouter.ai/keys)
OPENROUTER_API_KEY=
