# Authentication
AUTH_SECRET=                  # Required for NextAuth
AUTH_SYNC_SECRET=             # Secret for synchronizing user data with backend
AUTH_GITHUB_ID=               # GitHub OAuth Client ID
AUTH_GITHUB_SECRET=           # GitHub OAuth Client Secret
AUTH_JWT_ISSUER=catwalk-live
AUTH_JWT_AUDIENCE=catwalk-live-backend

# API Configuration
NEXT_PUBLIC_API_URL=          # Public URL for frontend (e.g., http://localhost:3000/api)
NEXT_PUBLIC_BACKEND_URL=      # Backend base URL (e.g., http://localhost:8000)
BACKEND_URL=                  # Server-side Backend base URL (defaults to NEXT_PUBLIC_BACKEND_URL)
